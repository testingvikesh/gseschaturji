config.json
.env
.env.local
public/uploads
output
node_modules

# cPanel-managed files (must stay out of git or deploy stays blocked)
php.ini
.user.ini
.htaccess

# cPanel / server files
.well-known
.ftpquota
.cpanel
.htpasswds
*.log
error_log
.git/index.lock

# legacy / other site folders on shared hosting
cgi-bin
gseschaturji.com
